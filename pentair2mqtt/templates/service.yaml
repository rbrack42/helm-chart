apiVersion: v1
kind: Service
metadata:
  name: {{ include "pentair2mqtt.fullname" . }}
  labels:
    {{- include "pentair2mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pentair2mqtt.selectorLabels" . | nindent 4 }}
