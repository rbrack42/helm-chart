apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pentair2mqtt.fullname" . }}-test-connection"
  labels:
    {{- include "pentair2mqtt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pentair2mqtt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
